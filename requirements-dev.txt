# Development / CI dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
ruff>=0.6

# Parses the SQL the app generates so the PostgreSQL dialect can be checked
# without a PostgreSQL server (see tests/test_postgres_dialect.py).
sqlglot>=25.0
